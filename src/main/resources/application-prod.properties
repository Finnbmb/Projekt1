# Produktions-Konfiguration - Azure MySQL Standard
server.port=8080

# Azure MySQL Datenbank (Flexible Server) - Direkte Konfiguration
spring.datasource.url=jdbc:mysql://dbotk25.mysql.database.azure.com:3306/terminkalender?useSSL=true&requireSSL=false&serverTimezone=UTC
spring.datasource.username=fbmb16
spring.datasource.password=PalKauf91
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Konfiguration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true

# Logging
logging.level.org.springframework=INFO
logging.level.de.swtp1.terminkalender=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Security
server.error.include-stacktrace=never
server.error.include-message=always

# CORS für Produktion
cors.allowed-origins=${CORS_ORIGINS:https://yourdomain.com}

# Management Endpoints (Actuator)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.health.mail.enabled=false

# Mail Configuration für Passwort-Reset (deaktiviert für Azure MySQL Setup)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
